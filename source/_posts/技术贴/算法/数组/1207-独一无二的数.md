---
title: 1207-独一无二的数
tags:
  - java
categories:
  - technical
  - leetcode
toc: true
date: 2020-10-28 13:56:15
---

## 题目描述

#### [1207. 独一无二的出现次数](https://leetcode-cn.com/problems/unique-number-of-occurrences/)

难度： 简单

给你一个整数数组 `arr`，请你帮忙统计数组中每个数的出现次数。

如果每个数的出现次数都是独一无二的，就返回 `true`；否则返回 `false`。

 <!-- more -->

**示例 1：**

```
输入：arr = [1,2,2,1,1,3]
输出：true
解释：在该数组中，1 出现了 3 次，2 出现了 2 次，3 只出现了 1 次。没有两个数的出现次数相同。
```

**示例 2：**

```
输入：arr = [1,2]
输出：false
```

**示例 3：**

```
输入：arr = [-3,0,1,-3,1,1,1,-3,10,0]
输出：true
```

 

**提示：**

- `1 <= arr.length <= 1000`
- `-1000 <= arr[i] <= 1000`

## 解题思路及代码

将数字出现个数通过hash映射到数组中，对于映射后的一维数组检查是否有重复。

```java
//方法一：映射
class Solution {
    public boolean uniqueOccurrences(int[] arr) {
        int[] tempArr = new int[2001];
        //映射
        for(int i=0; i<arr.length; i++){
            tempArr[arr[i]+1000]++;
        }
        //统计长度
        int length = 0;
        //用set去重，比较长度是否变化
        HashSet<Integer> set = new HashSet<>();
        for(int i=0; i<=2000; i++){
            if (tempArr[i]==0) continue;
            length++;
            set.add(tempArr[i]);
            if(set.size() < length)
                return false;
        }
        return true;
    }
}
```

> 作者：gumptlu
> 链接：https://leetcode-cn.com/problems/unique-number-of-occurrences/solution/hashying-she-by-gumptlu/
> 来源：力扣（LeetCode）
> 著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。