---
title: 151.翻转字符串里的单词
tags:
  - null
categories:
  - technical
  - leetcode
toc: true
date: 2022-01-17 21:44:10
---

## 题目描述

[151. 翻转字符串里的单词](https://leetcode-cn.com/problems/reverse-words-in-a-string/)

难度中等

给你一个字符串 `s` ，逐个翻转字符串中的所有 **单词** 。

**单词** 是由非空格字符组成的字符串。`s` 中使用至少一个空格将字符串中的 **单词** 分隔开。

请你返回一个翻转 `s` 中单词顺序并用单个空格相连的字符串。

**说明：**

- 输入字符串 `s` 可以在前面、后面或者单词间包含多余的空格。
- 翻转后单词间应当仅用一个空格分隔。
- 翻转后的字符串中不应包含额外的空格。

 <!-- more -->

**示例 1：**

```
输入：s = "the sky is blue"
输出："blue is sky the"
```

**示例 2：**

```
输入：s = "  hello world  "
输出："world hello"
解释：输入字符串可以在前面或者后面包含多余的空格，但是翻转后的字符不能包括。
```

**示例 3：**

```
输入：s = "a good   example"
输出："example good a"
解释：如果两个单词间有多余的空格，将翻转后单词间的空格减少到只含一个。
```

**示例 4：**

```
输入：s = "  Bob    Loves  Alice   "
输出："Alice Loves Bob"
```

**示例 5：**

```
输入：s = "Alice does not even like bob"
输出："bob like even not does Alice"
```

**提示：**

- `1 <= s.length <= 104`
- `s` 包含英文大小写字母、数字和空格 `' '`
- `s` 中 **至少存在一个** 单词

**进阶：**

- 请尝试使用 `*O*(1)` 额外空间复杂度的原地解法。

## 解题思路及代码

```go
// 栈
func reverseWords(s string) string {
    stack := make([]string, 0)
    // 扫描加入单词到队列
    word := ""
    for i, c := range s {
        if c == ' ' {
            if i < len(s)-1 && s[i+1] != ' ' {
                word = ""
            }else {
                continue
            }
        }else {
            word += string(c)
            if (i < len(s)-1 && s[i+1] == ' ') || i == len(s)-1 {
                // 如果是字母且下一个字符为空格或者为最后一个字符,那么就将此字母加入栈
                stack = append(stack, word)
            }
        }
    }
    // 从栈读取字符串
    ans := ""
    for len(stack) > 0 {
        word := stack[len(stack)-1]
        stack = stack[:len(stack)-1]
        ans = ans + word + " "
    }
    return ans[:len(ans)-1]
}
```

二刷:

```go
// 单词/标点(其他)tmp头部插入，空格插入在尾部
func reverseWords(s string) string {
    ans := ""
    tmp := ""       // 用于字符串的头插
    for i:=len(s)-1; i>=0; i-- {
        if s[i] == ' ' {
            // 尾部插入
            if len(ans) > 0 {
                ans += string(s[i])
            }
            // 将单词插入
            if len(tmp) > 0 {
                ans += tmp
                tmp = ""
            }
            // 到下一个非空格
            for i>=0 && s[i] == ' ' {
                i--
            }
            i++
        }else{
            // 使用tmp在头部插入
            tmp = string(s[i]) + tmp
        }
    }
    if len(tmp) > 0 {
        if len(ans) > 0  {
            ans += (" " + tmp)
        }else {
            ans += tmp
        }
    }
    return ans
}
```

栈:

```go
// 栈
func reverseWords(s string) string {
    stack := []string{}
    word := ""
    for i:=0; i<len(s); i++ {
        if s[i] == ' ' {
            // 将结果保存到栈
            if len(word) > 0 {
                stack = append(stack, word)
                word = ""
            }
            continue
        }
        // 组成word
        word += string(s[i])
    }
    if len(word) > 0 {
        stack = append(stack, word)
    }
    ans := ""
    // 遍历队列
    for len(stack) > 0 {
        if len(stack) > 1 {
            ans += (stack[len(stack)-1] + " ") 
        }else {
            ans += stack[0]
        }
        stack = stack[:len(stack)-1]
    }
    return ans
} 
```

